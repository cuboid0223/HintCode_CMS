import { User } from "@firecms/core";
import { CollectionEditorPermissions, PersistedCollection } from "@firecms/collection_editor";
import { UserManagement } from "@firecms/user_management";
export declare const RESERVED_GROUPS: string[];
export declare function resolveCollectionConfigPermissions<U extends User = User>({ user, userManagement, collection }: {
    user: User | null;
    userManagement: UserManagement<U>;
    collection?: PersistedCollection;
}): CollectionEditorPermissions;
export declare const areRolesEqual: (rolesA: string[], rolesB: string[]) => boolean;
