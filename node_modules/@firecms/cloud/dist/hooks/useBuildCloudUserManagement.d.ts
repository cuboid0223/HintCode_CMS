import { FirebaseApp } from "firebase/app";
import { FireCMSBackend, FireCMSCloudUserWithRoles } from "../types";
import { ProjectsApi } from "../api/projects";
import { UserManagement } from "@firecms/user_management";
interface UserManagementParams {
    backendFirebaseApp?: FirebaseApp;
    projectId: string;
    projectsApi: ProjectsApi;
    usersLimit?: number;
    canEditRoles?: boolean;
    fireCMSBackend: FireCMSBackend;
}
export declare function useBuildCloudUserManagement({ backendFirebaseApp, projectId, projectsApi, usersLimit, canEditRoles, fireCMSBackend }: UserManagementParams): UserManagement<FireCMSCloudUserWithRoles>;
export {};
