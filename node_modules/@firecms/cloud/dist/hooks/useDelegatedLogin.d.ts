import { FirebaseApp } from "firebase/app";
import { User as FirebaseUser } from "firebase/auth";
import { ProjectsApi } from "../api/projects";
import { ApiError } from "../types";
export type DelegatedLoginProps = {
    projectsApi: ProjectsApi;
    firebaseApp?: FirebaseApp;
    projectId: string;
    onUserChanged?: (user?: FirebaseUser) => void;
    onAnalyticsEvent?: (event: string, data?: object) => void;
};
export declare function useDelegatedLogin({ projectsApi, firebaseApp, projectId, onUserChanged, }: DelegatedLoginProps): {
    loginSuccessful: boolean;
    delegatedLoginLoading: boolean;
    delegatedLoginError: ApiError | Error | undefined;
};
