import { SearchIndex } from "algoliasearch";
import { FirestoreTextSearchControllerBuilder } from "../types";
/**
 * Utility function to perform a text search in an algolia index,
 * returning the ids of the entities.
 * @param index
 * @param query
 * @group Firebase
 */
export declare function performAlgoliaTextSearch(index: SearchIndex, query: string): Promise<readonly string[]>;
export declare function buildAlgoliaSearchController({ isPathSupported, search }: {
    isPathSupported: (path: string) => boolean;
    search: (props: {
        searchString: string;
        path: string;
    }) => Promise<readonly string[]> | undefined;
}): FirestoreTextSearchControllerBuilder;
